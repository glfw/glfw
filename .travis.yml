language: cpp

os:
    - linux
    - osx

sudo: false
addons:
    apt:
        sources:
            - kubuntu-backports
        packages:
            - cmake
script:
    - cmake .
    - cmake --build .
